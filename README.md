# 7tics.com

AI-native quant portfolio. Live trading systems + agent ops.

## Run locally

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Test

```bash
npm test
```

## Deploy

Push to `main` → Railway auto-deploys → Cloudflare DNS routes `7tics.com`.
